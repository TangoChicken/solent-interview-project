import styled from "styled-components";

export const StaffProfileContainer = styled.section`
  display: flex;
  width: 100%;
  max-width: var(--max-width);
  min-width: var(--min-width);
  min-height:100vh;
  margin: 0 auto;

  background: var(--lighter);

  flex-direction: column;
  align-items: center;
`;